# Curso Full Cycle

This challenge uses Docker containers for a quick and easy setup. Follow the instructions below to get started.

## Starting the Containers

To start the containers in detached mode, open your terminal and run:

```bash
  $ docker compose up -d
```

## Testing the Setup

Once the containers have started, you can test the application by visiting: http://localhost:8080